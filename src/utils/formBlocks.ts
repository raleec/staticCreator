/**
 * @deprecated This module has been superseded by `htmlSnippets.ts`.
 * It is kept as an empty stub to avoid breaking any external references.
 */
export function registerFormBlocks(): void {
  // no-op – GrapesJS has been replaced by the Webstudio-based HTML/CSS editor
}
